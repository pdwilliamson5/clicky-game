import React from "react";
import "./Title.css";

const Title = props =>(
    <div ClassName="titlebox">
    <h1>Kitty Clicky Game!</h1>
    <p>Click on a cat to earn points, but dont click more than once!</p>
    </div>
);

export default Title;